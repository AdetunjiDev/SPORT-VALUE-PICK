{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "dockerfilePath": "services/crawler/Dockerfile"
  },
  "deploy": {
    "preDeployCommand": ["cd packages/db && pnpm exec prisma migrate deploy"]
  }
}
